<div class="container">
  <ul>
    <% @posts.each do |post| %>
      <li><a href = <%="/posts/#{post.id}"%> ><%= post.title %></a>
        <br>
        <%= post.body %></li>
    <% end %>
  </ul>
</div>
